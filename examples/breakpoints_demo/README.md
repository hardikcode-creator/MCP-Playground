# breakpoints_demo

Shows **breakpoints / step-through** execution: the run pauses at a flagged node
so you can inspect its inputs/outputs before continuing.

- Config: `../_shared_configs/mcp-config-multi.json` (provides the `everything` server)
- Workflow: `workflow-with-breakpoint.json`
- Prereq: Node.js >= 18
