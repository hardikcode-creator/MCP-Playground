{
  "currentNode": {
    "tool": "vm__get_details",
    "description": "Fetch full details for a single virtual machine by its unique id.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "vmId": { "type": "string", "description": "Unique id of the VM to fetch." },
        "clusterId": { "type": "string", "description": "Id of the cluster the VM belongs to." },
        "includeMetrics": { "type": "boolean", "description": "Whether to include live metrics." }
      },
      "required": ["vmId", "clusterId"]
    }
  },
  "previousNodes": [
    {
      "nodeId": "list",
      "tool": "vm__list_vms",
      "description": "Lists VMs with their identifiers and placement.",
      "response": {
        "structuredContent": {
          "vms": [
            { "uuid": "vm-abc-123", "displayName": "web-1", "host": { "clusterUuid": "cl-prod-9", "name": "esx-7" }, "powerState": "ON" },
            { "uuid": "vm-def-456", "displayName": "db-1", "host": { "clusterUuid": "cl-prod-9", "name": "esx-8" }, "powerState": "OFF" }
          ],
          "total": 2
        }
      }
    }
  ]
}
